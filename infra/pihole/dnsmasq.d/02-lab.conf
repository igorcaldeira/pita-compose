# Wildcard: every *.lab.local resolves to the Caddy reverse proxy (10.10.0.3).
# Caddy then routes by Host header to the right service.
# To add a new service: just add a block in infra/Caddyfile — no DNS change needed.
address=/lab.local/10.10.0.3
